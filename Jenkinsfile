pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Ou para o nosso Jenkins"'
                sh '''
                    echo "Minha primeira pipeline"
                    ls -la
                '''
            }
        }
    }
}